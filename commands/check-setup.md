---
description: "Check the AI Employee Builder installation and fix what's broken — connector, license, plugin version, workspace scaffold. Safe to run any time; changes nothing without asking."
---

Run the bundled `check-setup` skill now: follow the instructions in the `check-setup` skill that ships with this plugin (it is stored locally, so it works even when the connection itself is broken). Diagnose in its order — plugin present, connector reachable, license state, workspace scaffold — then walk the user through the smallest fix for whatever is broken. Do not guess at fixes the skill doesn't prescribe.
