---
name: install-skill
description: Install or update skills through the licensed AIEB MCP runtime.
---

# Install Skill Stub

When this skill is invoked, call the `aieb` MCP tool `get_skill` with `skill_id` set to `install-skill` and `path` set to `SKILL.md`.

Follow the returned instructions. If additional files are needed, fetch them by calling `get_skill` with the same `skill_id` and the requested relative `path`.
