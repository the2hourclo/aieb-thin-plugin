---
name: meta-create-command
description: Create slash commands and quick actions through the licensed AIEB MCP runtime.
---

# Create Command Stub

When this skill is invoked, call the `aieb` MCP tool `get_skill` with `skill_id` set to `meta-create-command` and `path` set to `SKILL.md`.

Follow the returned instructions exactly, fetching any referenced support files through `get_skill`.
