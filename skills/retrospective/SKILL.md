---
name: retrospective
description: Capture learnings and improve skills through the licensed AIEB MCP runtime.
---

# Retrospective Stub

When this skill is invoked, call the `aieb` MCP tool `get_skill` with `skill_id` set to `retrospective` and `path` set to `SKILL.md`.

Follow the returned instructions and fetch any referenced support files through `get_skill`.
