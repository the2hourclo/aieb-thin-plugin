---
name: business-x-ray
description: Map and diagnose business operations through the licensed AIEB MCP runtime.
---

# Business X-Ray Stub

When this skill is invoked, call the `aieb` MCP tool `get_skill` with `skill_id` set to `business-x-ray` and `path` set to `SKILL.md`.

Use `get_skill` again for any workflow, reference, template, or script path named by the returned instructions. Do not invent missing AIEB source files locally.
