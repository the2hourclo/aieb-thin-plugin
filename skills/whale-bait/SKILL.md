---
name: whale-bait
description: "Writing employee · The Whale Bait lens — turn a content idea or a perspective shift into content that attracts the few RIGHT buyers (whales) instead of a broad crowd (fish), and generate fresh whale-bait ideas from the brand. Diagnoses fish-vs-whale and value-vs-insight, then charges the idea with the brand's Plan/Code/Ideas/Proof. USE WHEN user says 'make this whale bait', 'whale-bait this', 'is this fish bait', 'will this attract whales', 'better leads not more leads', 'give me whale bait ideas', 'content ideas for buyers', 'this attracts the wrong people', 'make this attract buyers not browsers', or hands over an idea / perspective-shift card to sharpen for the right audience. Runs AFTER perspective-shift (charges the shift it scoped) and BEFORE the format skill (write / carousel / reel-creator). Do NOT use to: render a finished piece (→ that format's skill), find the shift itself (→ perspective-shift), tighten a single claim (→ precision-principle), or write a full argument (→ writing-logic)."
user-invocable: false
---

# whale-bait — MCP loader

Load the licensed instructions before doing this job:

`get_skill(skill_id="whale-bait", path="SKILL.md", section="contents")`

The tool prefix varies by host. Search available and lazy-loaded tools for any name ending in `get_skill`. If none exists, run the local `check-setup` skill and follow its repair. Do not improvise install commands or versions, and never ask for a license key in chat.

Follow the returned routing map. Fetch only the section this run needs, then fetch each workflow, reference, template, example, or script it names with the same `skill_id` and exact remote path. Those files live on the AIEB server, not local disk. If a fetched instruction says to run a script, fetch it first and follow its safety instructions.

If the server returns a 🔒, entitlement, or setup message, relay its explanation and next action once. Do not retry a locked call, reconstruct paid content, or claim setup is fixed before a live fetch succeeds.
