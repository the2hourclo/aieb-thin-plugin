---
name: meta-create-agent
description: Create specialist agents through the licensed AIEB MCP runtime.
---

# Create Agent Stub

When this skill is invoked, call the `aieb` MCP tool `get_skill` with `skill_id` set to `meta-create-agent` and `path` set to `SKILL.md`.

Treat the returned instruction body as authoritative. If the server denies access, do not continue from memory.
