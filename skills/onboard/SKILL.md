---
name: onboard
description: Onboard users into AI Employee Builder through the licensed AIEB MCP runtime.
---

# Onboard Stub

When this skill is invoked, call the `aieb` MCP tool `get_skill` with `skill_id` set to `onboard` and `path` set to `SKILL.md`.

Use the returned onboarding instructions. If access is denied, show the returned help message and do not expose local paid onboarding content.
