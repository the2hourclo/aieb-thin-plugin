---
name: meta-create-mcp
description: Build or configure MCP servers through the licensed AIEB MCP runtime.
---

# Create MCP Stub

When this skill is invoked, call the `aieb` MCP tool `get_skill` with `skill_id` set to `meta-create-mcp` and `path` set to `SKILL.md`.

Follow the returned instructions and fetch any referenced workflows, references, templates, or scripts from the same MCP server.
