---
name: excali-graphic
description: "Writing employee · Render legible hand-drawn 'Excalidraw whiteboard' diagrams, graphs, and charts as PNGs from hand-authored HTML/SVG — white background, real Excalifont, an SVG wobble filter on SHAPES ONLY so text stays crisp, color-by-meaning accents. The go-to recipe for email graphs, newsletter diagrams, process maps, and video teaching slides. USE WHEN user says 'make a graph for my email', 'excalidraw diagram', 'whiteboard graphic', 'hand-drawn diagram', 'diagram this', 'make a chart', 'draw a process diagram', 'visualize this as a graph', 'teaching-slide graphic', 'sketch this concept on a board', or wants a clean hand-drawn diagram/graph for a video slide, email, newsletter, carousel slide, or canvas. Do NOT use for: photoreal/AI-generated imagery (outside this plugin's scope), or a full multi-slide social carousel (→ carousel — though its individual diagram slides can be drawn HERE)."
---

# excali-graphic — MCP loader

Load the licensed instructions before doing this job:

`get_skill(skill_id="excali-graphic", path="SKILL.md", section="contents")`

The tool prefix varies by host. Search available and lazy-loaded tools for any name ending in `get_skill`. If none exists, run the local `check-setup` skill and follow its repair. Do not improvise install commands or versions, and never ask for a license key in chat.

Follow the returned routing map. Fetch only the section this run needs, then fetch each workflow, reference, template, example, or script it names with the same `skill_id` and exact remote path. Those files live on the AIEB server, not local disk. If a fetched instruction says to run a script, fetch it first and follow its safety instructions.

If the server returns a 🔒, entitlement, or setup message, relay its explanation and next action once. Do not retry a locked call, reconstruct paid content, or claim setup is fixed before a live fetch succeeds.
