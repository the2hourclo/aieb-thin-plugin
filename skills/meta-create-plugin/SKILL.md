---
name: meta-create-plugin
description: Package skills and integrations into plugins through the licensed AIEB MCP runtime.
---

# Create Plugin Stub

When this skill is invoked, call the `aieb` MCP tool `get_skill` with `skill_id` set to `meta-create-plugin` and `path` set to `SKILL.md`.

Use the returned paid instructions as the source of truth. If the MCP gate refuses access, stop and show the returned renewal/help message.
