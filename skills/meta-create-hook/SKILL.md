---
name: meta-create-hook
description: Create Codex automation hooks through the licensed AIEB MCP runtime.
---

# Create Hook Stub

When this skill is invoked, call the `aieb` MCP tool `get_skill` with `skill_id` set to `meta-create-hook` and `path` set to `SKILL.md`.

Use the returned instructions as the full skill. Do not rely on local paid hook-building instructions.
